#include <stdio.h>
#include<math.h>
int main() 
{
double a,b,c;
double x,y;
printf("enter the value of roots");
scanf("%lf%lf%lf",&a,&b,&c);
x=-b+(sqrt(b*b-4*a*c))/(2*a);
y=+b-(sqrt(b*b-4*a*c))/(2*a);
printf("%lf",x);
printf("%lf",y);
return 0;
}
